{% if form.vars.errors|length > 0 %}
    <div class="alert alert--error">
        <ul class="error-list">
            {% for error in form.vars.errors %}
                <li>{{ error.message }}</li>
            {% endfor %}
        </ul>
    </div>
{% endif %}
