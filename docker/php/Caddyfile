{
    {$CADDY_GLOBAL_OPTIONS}

    frankenphp {
        {$FRANKENPHP_CONFIG}
    }
}

http://{$SERVER_NAME:localhost} {
    log {
        level INFO
        output stderr
        format filter {
            wrap console
            fields {
                uri query {
                    replace _profiler *
                }
            }
        }
    }

    root * /app/public

    encode zstd br gzip

    # Upload size limit
    request_body {
        max_size 20MB
    }

    php_server {
        index index.php
        resolve_root_symlink
    }
}
